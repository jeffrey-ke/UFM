defaults:
  - default
  - override quantity_options/train: optflow_finetune
  - override quantity_options/val: optflow_finetune
  - override optflow_isaac/train: finetune

# Fixed single AR (~16:9, matches 1080×1920 renders) — no multi-AR bucket pool.
resolution_train: ${dataset.resolution_options.560_1_77_ar}
resolution_val: ${dataset.resolution_options.560_1_77_ar}
